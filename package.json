{
  "name": "tractor",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run web-deal-service.ts",
    "dev:frontend": "cd webapp && bun run dev",
    "dev:all": "bun run web-deal-service.ts & cd webapp && bun run dev",
    "test": "bun test",
    "build:webapp": "cd webapp && bun run build",
    "start": "bun run web-deal-service.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
